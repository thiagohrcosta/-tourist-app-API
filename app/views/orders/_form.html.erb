<%= simple_form_for [@ticket, @order] do |f| %>
  <%= f.input :quantity, label: "Unidades" %>
  <% f.input :user_id = current_user.id %>
  <%= f.association :ticket %>
  <%= f.submit "Comprar", class: "btn btn-create w-100" %>

<% end %>
